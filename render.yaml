services:
  - type: web
    name: ai-fridge-to-recipe
    env: node
    buildCommand: npm run postinstall
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 5000
